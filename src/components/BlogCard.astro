---
import type { BlogPost } from "@/data/blogPosts";

interface Props {
  post: BlogPost;
}

const { post } = Astro.props;
const formattedDate = new Date(post.date).toLocaleDateString("en-US", {
  year: "numeric",
  month: "long",
  day: "numeric",
});
---

<div class="bg-card rounded-2xl shadow-sm border border-border hover:shadow-md transition-shadow overflow-hidden flex flex-col">
  <div class="p-6 flex flex-col flex-1">
    <p class="text-xs text-muted-foreground mb-2">{formattedDate}</p>
    <h3 class="text-lg font-semibold text-primary mb-3 leading-snug">
      {post.title}
    </h3>
    <p class="text-foreground/70 leading-relaxed text-sm mb-6 flex-1">
      {post.excerpt}
    </p>
    <a
      href={`/blog/${post.slug}`}
      aria-label={`Read more about ${post.title}`}
      class="inline-flex items-center justify-center rounded-full bg-primary px-6 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90 transition-opacity self-start"
    >
      Read More
    </a>
  </div>
</div>
